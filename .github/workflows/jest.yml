name: Run Jest Testes

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Registrar horário de início
        run: echo "START_TIME=$(date +%s)" >> $GITHUB_ENV
        
      - name: 🚀 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🔧 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 📦 Instalar dependências
        run: npm install

      - name: 🧪 Rodar os testes com Jest
        run: npm test
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}

      - name: 📂 Upload das evidências de teste
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: coverage

      - name: Calcular duração dos testes
        run: |
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "DURATION=$DURATION" >> $GITHUB_ENV

      - name: Enviar notificação para Discord
        if: always()
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          JOB_NAME: "Test Execution"
        run: |
          # Recarregar variáveis do GITHUB_ENV
          source $GITHUB_ENV
          
          MESSAGE=$(jq -n \
            --arg username "GitHub Actions" \
            --arg avatar_url "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png" \
            --arg job "**🚀 Teste Concluído!**" \
            --arg testname "📝 **Nome do Teste:** $JOB_NAME" \
            --arg duration "⏱ **Duração:** ${DURATION} segundos" \
            --arg link "🔗 [Veja no GitHub](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
          )

          curl -H "Content-Type: application/json" \
               -X POST \
               -d "$MESSAGE" \
               $DISCORD_WEBHOOK